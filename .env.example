# Optional overrides for docker compose (copy to `.env` in the repo root).
# Defaults match apps/api/.env.example — fine for local development only.

POSTGRES_USER=balance
POSTGRES_PASSWORD=balance
POSTGRES_DB=balance
POSTGRES_PORT=5432
